/**
 * Modal Components Index
 *
 * Re-exports modal components for backward compatibility.
 * Modals are overlay components that display content on top of the main page.
 *
 * Note: PortfolioModal is currently internal to PageViews.tsx.
 * Future work: Extract into a separate file for better modularity.
 */

// PortfolioModal is currently not exported from PageViews.tsx
// It's used internally within PortfolioView
// Future: Extract to separate file and export here
