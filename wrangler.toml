# Cloudflare Pages configuration
name = "bryanvrgsc-portfolio"
compatibility_date = "2024-12-11"

# Pages specific settings
pages_build_output_dir = "dist"

# Note: For Pages, most settings are configured in the Cloudflare dashboard
# Environment variables should be set in:
# Cloudflare Dashboard > Workers & Pages > Your Project > Settings > Environment variables
#
# Required variables:
# - PUBLIC_FORMSPREE_ID (if using contact form)
# - PUBLIC_RECAPTCHA_SITE_KEY (if using reCAPTCHA)
